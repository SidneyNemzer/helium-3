apps:
  - name: helium3
    script: ./build/index.js
    env_production:
      NODE_ENV: production
      PORT: 80
    min_uptime: 2000
    kill_timeout: 2000
    max_restarts: 5
    restart_delay: 1000
    # Output timestamps in logs
    time: true
